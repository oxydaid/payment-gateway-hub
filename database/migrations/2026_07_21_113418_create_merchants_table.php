<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::create('merchants', function (Blueprint $table) {
            $table->id();
            $table->string('name'); // Contoh: "Sistem Invoice", "Bazar Market"
            $table->string('webhook_url')->nullable(); // URL untuk menerima callback
            $table->string('api_key')->unique()->nullable(); // Custom API Key atau bisa diintegrasikan dengan Sanctum token
            $table->boolean('is_active')->default(true);
            $table->timestamps();
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::dropIfExists('merchants');
    }
};
